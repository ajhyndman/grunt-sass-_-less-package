1. Copy the contents of this directory into /wp-content/themes/dynamik/css/
2. Open the directory in a terminal
3. Run "npm install"
4. Run "grunt"

This will watch and compile any scss files that you create in the sass directory.
